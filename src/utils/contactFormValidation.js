/**
 * Contact form validation used in the browser before submit.
 * (Same rules as `shared/contactValidation.js` — the API repeats them for safety.)
 */
export { validateContactPayload } from '../../shared/contactValidation.js'
